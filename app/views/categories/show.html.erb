<h1><%= @category.name %> Listings</h1>
<h3><%=link_to "Homepage", main_path %></h3>
<% @category.listings.each do |listing| %>
<ul>
<li><%= link_to listing.title, listing_path(listing) %>  </li>
</ul>
<% end %>
